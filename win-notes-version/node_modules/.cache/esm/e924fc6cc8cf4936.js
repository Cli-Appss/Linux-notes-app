let minimist,help,version;_c8c‍.x([["cli",()=>cli]]);_c8c‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_c8c‍.w("./help.js",[["help",["help"],function(v){help=v}]]);_c8c‍.w("./version.js",[["version",["version"],function(v){version=v}]]);



       async function cli(argsArray) {
    const args = minimist(argsArray.slice(2));

    let cmd = args._[0] || 'help';

  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;

  }
}