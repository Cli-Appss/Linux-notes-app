let minimist,help,version,test;_55b‍.x([["cli",()=>cli]]);_55b‍.w("minimist",[["default",["minimist"],function(v){minimist=v}]]);_55b‍.w("./help.js",[["help",["help"],function(v){help=v}]]);_55b‍.w("./version.js",[["version",["version"],function(v){version=v}]]);_55b‍.w("./test.js",[["test",["test"],function(v){test=v}]]);




       async function cli(argsArray) {
    const args = minimist(argsArray.slice(2));

    let cmd = args._[0] || 'help';

  if (args.version || args.v) {
    cmd = 'version';
  }

  if (args.help || args.h) {
    cmd = 'help';
  }

  if(args.test || args.t){
      cmd = 'test'
  }

  switch (cmd) {
    case 'version':
      version(args);
      break;

    case 'help':
      help(args);
      break;
    
    case 'test':
        test(args);
        break;

  }


}